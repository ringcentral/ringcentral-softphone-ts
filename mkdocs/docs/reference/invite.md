# cloudPhone.on('invite', callback)

## Callback inputs

| Parameter   | Description |
|-------------|-------------|
| `message` |             |

## Sample

```ts
cloudPhone.on("invite", (message) => {
	// do something
});
```

